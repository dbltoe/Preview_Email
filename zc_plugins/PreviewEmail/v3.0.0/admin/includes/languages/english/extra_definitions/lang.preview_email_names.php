<?php
/**
 * Preview Email -- admin menu label (English).
 *
 * Referenced by the `language_key` the ScriptedInstaller writes into the
 * `admin_pages` table, so it must exist before the menus are drawn, hence
 * `extra_definitions`. Array form only: Zen Cart v3.0.0 no longer loads
 * define()-style language files.
 *
 * @package  PreviewEmail
 * @license  http://www.zen-cart.com/license/2_0.txt GNU Public License V2.0
 */

$define = [
    'BOX_TOOLS_PREVIEW_EMAIL' => 'Preview Email',
];

return $define;
